{
  "name": "bon-hiato",
  "version": "0.0.1",
  "description": "App for managing projects",
  "main": "dist/main.js",
  "scripts": {
    "build": "elm make src/Main.elm --output dist/elm.js || true",
    "watch": "chokidar-cmd -t '**/*.elm' -c 'npm run build'",
    "start": "electron dist/main.js"
  },
  "author": "Nikita Tchayka",
  "license": "MIT",
  "devDependencies": {
    "electron": "^1.3.1",
    "electron-prebuilt": "^1.3.1",
    "jquery": "^3.1.0"
  }
}
